//
// NED source-folder root for the regression test networks.
//
// Each network lives in networks/<name>/ and therefore declares
// package afdxtest.networks.<name>; OMNeT++ requires the declared package to
// match the directory path relative to this root.
//
package afdxtest;
